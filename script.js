// Aura Media Lab - Interactive Script

document.addEventListener('DOMContentLoaded', () => {
    const mainButton = document.querySelector('button');

    if (mainButton) {
        mainButton.addEventListener('click', () => {
            alert('Welcome to Aura Media Lab! We are preparing our AI services for you.');
        });
    }

    // Scroll animation effect (Optional)
    console.log("Aura Media Lab site is ready!");
});
